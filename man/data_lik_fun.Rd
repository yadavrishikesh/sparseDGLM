% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lik_data.R
\name{data_lik_fun}
\alias{data_lik_fun}
\title{Log Likelihood Calculation for Different GLM Families}
\usage{
data_lik_fun(Y, lambda, r, k, data_lik)
}
\arguments{
\item{Y}{A matrix representing the observed data, where rows correspond to time points and columns to spatial locations.}

\item{lambda}{A matrix representing the linear predictor for the GLM, with the same dimensions as `Y`.}

\item{r}{A numeric parameter for the Negative Binomial distribution, representing the dispersion (shape parameter).}

\item{k}{A numeric precision parameter for the Log-Normal distribution.}

\item{data_lik}{A character string specifying the likelihood family. Options include `"Poisson"`, `"NegB"` (Negative Binomial), and `"lNormal"` (Log-Normal).}
}
\value{
A matrix of log likelihood values with the same dimensions as `Y`.
}
\description{
This function computes the log likelihood of the data `Y` given the linear predictor `lambda` for various generalized linear model (GLM) families of distributions. It supports Poisson, Negative Binomial, and Log-Normal distributions.
}
\examples{
# Example with Poisson likelihood
Y <- matrix(rpois(20, lambda = 3), nrow = 4, ncol = 5)
lambda <- log(Y + 1)
log_lik_poisson <- data_lik_fun(Y, lambda, r = NULL, k = NULL, data_lik = "Poisson")
print(log_lik_poisson)

# Example with Negative Binomial likelihood
r <- 5
log_lik_negb <- data_lik_fun(Y, lambda, r, k = NULL, data_lik = "NegB")
print(log_lik_negb)

# Example with Log-Normal likelihood
k <- 2
log_lik_lnormal <- data_lik_fun(Y, lambda, r = NULL, k, data_lik = "lNormal")
print(log_lik_lnormal)
}
