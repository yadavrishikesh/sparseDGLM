% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lik_latent_lambda.R
\name{lambda_lik_fun}
\alias{lambda_lik_fun}
\title{Log Likelihood for Lambda with Gaussian Prior}
\usage{
lambda_lik_fun(lambda, mean.lambda, tau2)
}
\arguments{
\item{lambda}{A matrix or vector of the latent parameter values.}

\item{mean.lambda}{A matrix or vector representing the mean of the Gaussian prior.}

\item{tau2}{A numeric value representing the variance of the Gaussian prior.}
}
\value{
A matrix or vector of log likelihood values with respect to `lambda`.
}
\description{
This function computes the log likelihood of the latent parameter `lambda` under a Gaussian prior, with mean `mean.lambda` and variance `tau2`.
}
\examples{
\dontrun{
# Example usage:
lambda <- matrix(rnorm(10), nrow = 5, ncol = 2)
mean.lambda <- matrix(0, nrow = 5, ncol = 2)
tau2 <- 1
log_lik <- lambda_lik_fun(lambda, mean.lambda, tau2)
print(log_lik)
}
}
