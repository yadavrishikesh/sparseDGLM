% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_synthetic_data.R
\name{sim_synthetic_data}
\alias{sim_synthetic_data}
\title{Simulate Data from the DGLM Model}
\usage{
sim_synthetic_data(
  nt,
  model,
  data_lik,
  loc,
  mesh.inf = NULL,
  mesh.hyper = NULL,
  hyper,
  beta0,
  beta,
  Wt,
  X,
  Ft,
  G,
  cor.type,
  theta0,
  mu0,
  C0,
  R0
)
}
\arguments{
\item{nt}{Integer, number of time points.}

\item{model}{Character, specifying the model type. Options include `"bayes.reg"`, `"dense"`, and `"sparse"`.}

\item{data_lik}{Character, specifying the likelihood type. Options include `"Poisson"`, `"NegB"`, and `"lNormal"`.}

\item{loc}{A matrix of spatial coordinates for each spatial location.}

\item{mesh.inf}{A list containing mesh parameters (`max.edge`, `cutoff`, and `offset`). If set to `NULL`, the mesh is created automatically inside the function with default values.}

\item{hyper}{A list containing model-specific hyperparameters such as `kappa`, `sigma2`, `tau2`, and `r`.}

\item{beta0}{Numeric, the intercept for the regression model.}

\item{beta}{A numeric vector of regression coefficients.}

\item{X}{A 3D array of fixed covariates for the regression model.}

\item{Ft}{A matrix of dynamic temporal covariates.}

\item{G}{A matrix for updating dynamic temporal covariates over time.}

\item{cor.type}{Character, specifying the correlation type for the dense model.}

\item{theta0}{A numeric vector of initial values for the dynamic covariates.}

\item{mu0}{Numeric, the initial value for the spatial mean.}

\item{C0}{Numeric, the initial variance for the spatial model.}

\item{R0}{Numeric, the initial value for the spatial random field.}
}
\value{
A list containing simulated data, including the response `y`, linear predictor `lambda`, dynamic covariate `theta`, covariate effects, spatial random effects `mu.st`, and other model-specific outputs.
}
\description{
This function simulates data from a Dynamic Generalized Linear Model (DGLM) with different response variables and space-time structures in the predictors. The function supports multiple model types (`bayes.reg`, `dense`, and `sparse`) and allows for simulation from various likelihoods (`Poisson`, `NegB`, `lNormal`).
}
\examples{
\dontrun{
# Example usage:
nt <- 100
ns <- 50
loc <- matrix(runif(ns * 2), ncol = 2)
hyper <- list(kappa = 0.5, sigma2 = 1, tau2 = 0.1)
beta <- rnorm(5)
X <- array(rnorm(nt * ns * 5), dim = c(nt, ns, 5))
Ft <- matrix(rnorm(nt * 3), nt, 3)
G <- diag(3)
result <- sim_synthetic_data(nt, "dense", "Poisson", loc, hyper = hyper, beta = beta, X = X, Ft = Ft, G = G)
}
}
