% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lik_latent_lambda.R
\name{lambda_grad_fun}
\alias{lambda_grad_fun}
\title{Gradient of the Log Likelihood for Lambda with Gaussian Prior}
\usage{
lambda_grad_fun(lambda, mean.lambda, tau2)
}
\arguments{
\item{lambda}{A matrix or vector of the latent parameter values.}

\item{mean.lambda}{A matrix or vector representing the mean of the Gaussian prior.}

\item{tau2}{A numeric value representing the variance of the Gaussian prior.}
}
\value{
A matrix or vector representing the gradient of the log likelihood with respect to `lambda`.
}
\description{
This function computes the gradient of the log likelihood for the latent parameter `lambda` under a Gaussian prior with mean `mean.lambda` and variance `tau2`.
}
\examples{
\dontrun{
# Example usage:
lambda <- matrix(rnorm(10), nrow = 5, ncol = 2)
mean.lambda <- matrix(0, nrow = 5, ncol = 2)
tau2 <- 1
grad_lik <- lambda_grad_fun(lambda, mean.lambda, tau2)
print(grad_lik)
}
}
