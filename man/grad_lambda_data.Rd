% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lik_data.R
\name{grad_lambda_data}
\alias{grad_lambda_data}
\title{Gradient of the Log Likelihood with Respect to Lambda}
\usage{
grad_lambda_data(Y, lambda, r, k, data_lik)
}
\arguments{
\item{Y}{A matrix representing the observed data, where rows correspond to time points and columns to spatial locations.}

\item{lambda}{A matrix representing the linear predictor for the GLM, with the same dimensions as `Y`.}

\item{r}{A numeric parameter for the Negative Binomial distribution, representing the dispersion (shape parameter).}

\item{k}{A numeric precision parameter for the Log-Normal distribution.}

\item{data_lik}{A character string specifying the likelihood family. Options include `"Poisson"`, `"NegB"` (Negative Binomial), and `"lNormal"` (Log-Normal).}
}
\value{
A matrix of gradient values with respect to `lambda`, with the same dimensions as `Y`.
}
\description{
This function computes the gradient of the log likelihood with respect to the linear predictor `lambda` for various generalized linear model (GLM) families. It supports Poisson, Negative Binomial, and Log-Normal distributions.
}
\examples{
# Example with Poisson likelihood
Y <- matrix(rpois(20, lambda = 3), nrow = 4, ncol = 5)
lambda <- log(Y + 1)
grad_poisson <- grad_lambda_data(Y, lambda, r = NULL, k = NULL, data_lik = "Poisson")
print(grad_poisson)

# Example with Negative Binomial likelihood
r <- 5
grad_negb <- grad_lambda_data(Y, lambda, r, k = NULL, data_lik = "NegB")
print(grad_negb)

# Example with Log-Normal likelihood
k <- 2
grad_lnormal <- grad_lambda_data(Y, lambda, r = NULL, k, data_lik = "lNormal")
print(grad_lnormal)
}
